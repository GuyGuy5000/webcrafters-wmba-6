<br />
<div class="card mb-sm-3" style="min-width: 18rem;">
    <h4 class="card-header" id="GameLocation">Game Location</h4>
    <div class="card-body">
        <form asp-action="InsertFromExcel" asp-controller="GameLocation" method="post" enctype="multipart/form-data">
            <div class="row">
                <div class="col-md-1"></div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label class="control-label custom-file-label">Upload Excel File:</label>
                        <div class="input-group mb-3">
                            <input type="file" class="form-control" name="theExcel" id="theExcel">
                        </div>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Upload Data" class="btn btn-outline-dark" />
                    </div>
                </div>
                <div class="col-md-7">
                    <p>
                        Put all the data to upload into the first column of the Excel spreadsheet.<br />
                        Start the data in the second row of your spreadsheet to leave space for the <br />headings "Name" in the first row.<br />
                        The program looks for these column headings to help make sure you uploaded the correct file.
                    </p>
                </div>
            </div>
        </form>
        @Html.DropDownList("GameLocationID", null, htmlAttributes: new { @size = 6, @class = "form-control" })
    </div>

    <div class="card-footer">
        <a asp-controller="GameLocation" asp-action="Create" class="btn btn-outline-dark">Create</a> 
        <a asp-controller="GameLocation" asp-action="Edit" class="btn btn-outline-dark linkClick">Edit</a> 
        <a asp-controller="GameLocation" asp-action="Delete" class="btn btn-outline-dark linkClick">Delete</a>
    </div>
</div>
